package com.ubo.tp.message.ihm.component;

/**
 * Interface marqueur pour tous les composants UI (composants purs, réutilisables).
 *
 * Cette interface permet d'exprimer explicitement dans les APIs que la
 * factory de vues retourne une ou plusieurs unités UI de type composant.
 *
 * Elle est volontairement vide (marqueur) afin de rester légère ; elle
 * peut être enrichie plus tard (ex: lifecycle methods) si nécessaire.
 */
public interface Component {
}

