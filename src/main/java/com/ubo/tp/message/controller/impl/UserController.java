package com.ubo.tp.message.controller.impl;

import com.ubo.tp.message.controller.service.IUserController;

public class UserController implements IUserController{

    @Override
    public void refreshUsers() {
        // implémentation par défaut vide (le comportement réel peut être ajouté plus tard)
    }

    @Override
    public void onUserSelected(String userTag) {
        // implémentation par défaut vide
    }
}
